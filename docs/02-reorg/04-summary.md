# 重组摘要

重组完成后的目标不是“看起来更高级”，而是：

- `session` 只做运行状态机
- `runtime` 只做依赖装配
- `tools` 只做能力定义和执行
- `handler` 只做桥接
- `service` 只做领域承接
- `repository` 只做存储

## 判断重组是否成功

以后你在加一个新 feature 时，应该能快速回答：

1. 这个状态属于哪一层
2. 哪个模块拥有真相
3. 哪些模块只是配合
4. 横切逻辑放哪里

如果还是每次都要到处翻文件，说明重组还不够。

